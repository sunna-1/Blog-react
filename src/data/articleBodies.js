export const articleBodies = {
  garden: `## 为什么是「数字花园」

博客不该是一次性装修。更像花园：今天种一棵，明天修一行，时间久了自然有层次。

### 三个原则

1. **结构先简单** — 首页、文章、日常、工具，四个入口足够。
2. **内容可迁移** — Markdown 存在仓库里，随时能换 CMS。
3. **部署要省心** — Cloudflare Pages 免费层 + 静态构建，足够个人站。

### 从模板到日记本

最初是清爽的技术博客模板；后来换成手绘本视觉，因为这里首先是**日记**，其次才是展示。

> 页面好看很重要，但更重要的是：你愿意回来继续写。

### 下一步

- 文章详情与工具页完善
- 更多 SVG 生活插画
- 按需接入评论或统计（仍保持静态优先）
`,

  cloudflare: `## 构建配置

在 Cloudflare Pages 控制台中：

| 项 | 值 |
|---|---|
| Build command | \`npm run build\` |
| Output directory | \`dist\` |
| Root | \`/\` |

## SPA 路由

React Router 需要让所有路径回退到 \`index.html\`。在 \`public/_redirects\` 中：

\`\`\`
/*    /index.html   200
\`\`\`

## 推 main 会不会自动上线？

若项目已 **Connect to Git** 且生产分支为 \`main\`，推送成功并构建通过后，**生产站会更新**。

若仅手动上传 \`dist\`，则推送 Git 不会自动改线上，需要重新部署。

## 本地验证

\`\`\`bash
npm run build
npm run preview
\`\`\`

预览通过后再推送，可减少线上试错。
`,

  modules: `## 数据与页面分离

当前文章、日常、名言、工具配置集中在 \`src/data/\`：

- \`content.js\` — 元信息与列表
- \`articleBodies.js\` — 文章正文 Markdown

页面只负责展示，改内容不必动组件逻辑。

## 为什么不用大而全的 CMS

个人博客的更新频率有限，**Git + Markdown** 的好处是：

- 版本可追溯
- 无数据库成本
- 完全兼容 Cloudflare 免费静态托管

## 扩展路径

当文章变多，可以：

1. 按年份拆分 \`articleBodies\`
2. 构建时从文件夹读取 \`.md\`（Vite glob import）
3. 再接 Headless CMS，页面组件几乎不用改
`,

  writing: `## 三阶段写作

### 1. 收集（Collect）

不评判，只记录：链接、半句话、截图说明。目标是降低「空白页」压力。

### 2. 判断（Curate）

每周看一遍收集箱，只留 1～2 个值得展开的题。其余删掉或归档。

### 3. 表达（Compose）

用 Markdown 先写骨架：标题、三个小标题、每段一句核心话。再润色字句。

## 日记 vs 文章

| 类型 | 长度 | 语气 |
|------|------|------|
| 日常 | 短 | 随意、当下 |
| 文章 | 长 | 结构清晰、可回看 |

本站侧边栏把两者分开，避免「什么都像论文」。

## 工具支持

站内 **Markdown 预览** 适合起草；写技术文时可用 **复杂度评估** 辅助整理思路（启发式，需人工复核）。
`,
}
